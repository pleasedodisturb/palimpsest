---
phase: 01-package-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - palimpsest/__init__.py
  - palimpsest/cli/__init__.py
  - palimpsest/cli/main.py
  - palimpsest/content/__init__.py
  - palimpsest/content/docs/
  - palimpsest/content/templates/
  - palimpsest/content/agents/
  - palimpsest/content/playbooks/
  - palimpsest/content/showcase/
  - palimpsest/content/case-study/
autonomous: true
requirements:
  - CLI-01
  - DIST-02

must_haves:
  truths:
    - "pip install -e . succeeds and registers pal, palimpsest, and pac entry points"
    - "palimpsest/content/ contains all methodology docs, templates, agents, playbooks, showcase, and case-study files"
    - "importlib.resources can locate and read files from palimpsest.content at runtime"
    - "Existing scripts/tests/ test suite still passes after restructuring"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated package config with entry points, dependencies, and package-data"
      contains: "pal = \"palimpsest.cli.main:run\""
    - path: "palimpsest/__init__.py"
      provides: "Root package marker"
      contains: "__version__"
    - path: "palimpsest/cli/__init__.py"
      provides: "CLI subpackage marker"
    - path: "palimpsest/cli/main.py"
      provides: "Minimal entry point function (run) for console_scripts"
      exports: ["run"]
    - path: "palimpsest/content/__init__.py"
      provides: "Content access helpers using importlib.resources"
      exports: ["get_content_path", "list_content"]
    - path: "palimpsest/content/docs/"
      provides: "Bundled methodology guides"
      min_files: 10
    - path: "palimpsest/content/templates/"
      provides: "Bundled TPM templates"
      min_files: 7
    - path: "palimpsest/content/agents/"
      provides: "Bundled agent configurations"
      min_files: 5
    - path: "palimpsest/content/playbooks/"
      provides: "Bundled TPM playbooks"
      min_files: 4
  key_links:
    - from: "pyproject.toml"
      to: "palimpsest.cli.main:run"
      via: "[project.scripts] entry points"
      pattern: "pal = \"palimpsest\\.cli\\.main:run\""
    - from: "pyproject.toml"
      to: "palimpsest/content/"
      via: "[tool.setuptools.package-data] globs"
      pattern: "palimpsest\\.content"
    - from: "palimpsest/content/__init__.py"
      to: "palimpsest/content/docs/"
      via: "importlib.resources.files()"
      pattern: "importlib\\.resources"
---

<objective>
Restructure the repository into an installable Python package with all methodology content bundled as package data.

Purpose: After this plan, `pip install -e .` produces a working package with `pal`, `palimpsest`, and `pac` entry points, and all content (docs, templates, agents, playbooks, showcase, case-study) is accessible via `importlib.resources`. This is the foundation everything else builds on.

Output: Updated pyproject.toml, palimpsest/ package directory with content subdirectories, minimal CLI entry point stub.
</objective>

<execution_context>
@/Users/pleasedodisturb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pleasedodisturb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Current pyproject.toml entry point (will be preserved alongside new ones): -->
<!-- pac = "scripts.core.preflight_check:main" -->

<!-- New entry points to create: -->
<!-- pal = "palimpsest.cli.main:run" -->
<!-- palimpsest = "palimpsest.cli.main:run" -->
<!-- pac = "palimpsest.cli.main:run"  (replaces old pac) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package structure and copy content into palimpsest/content/</name>
  <files>
    palimpsest/__init__.py,
    palimpsest/content/__init__.py,
    palimpsest/content/docs/ (copied from docs/),
    palimpsest/content/templates/ (copied from templates/),
    palimpsest/content/agents/ (copied from agents/),
    palimpsest/content/playbooks/ (copied from playbooks/),
    palimpsest/content/showcase/ (copied from showcase/),
    palimpsest/content/case-study/ (copied from case-study/),
    palimpsest/cli/__init__.py,
    palimpsest/cli/main.py
  </files>
  <read_first>
    pyproject.toml,
    docs/ (list files),
    templates/ (list files),
    agents/ (list files recursively),
    playbooks/ (list files),
    showcase/ (list files),
    case-study/ (list files)
  </read_first>
  <action>
1. **Clean stale build artifacts:**
   - Delete `palimpsest.egg-info/` directory (stale SOURCES.txt will cause phantom files)
   - Delete `palimpsest/__pycache__/` and any existing `palimpsest/data/` and `palimpsest/scripts/` (these are build cache from prior installs, not source)

2. **Create palimpsest/__init__.py:**
```python
"""Palimpsest — AI-augmented Technical Program Management toolkit."""

__version__ = "0.1.0"
```

3. **Create palimpsest/cli/__init__.py:**
```python
"""CLI subpackage for the pal command."""
```

4. **Create palimpsest/cli/main.py** (minimal stub — full implementation in Plan 02):
```python
"""Entry point for pal / palimpsest CLI."""

import typer

app = typer.Typer(
    name="pal",
    help="AI-augmented Technical Program Management toolkit.",
    no_args_is_help=True,
)

@app.callback()
def callback() -> None:
    """AI-augmented Technical Program Management toolkit."""
    pass

def run() -> None:
    """Entry point for console_scripts."""
    app()
```

5. **Copy content directories** into `palimpsest/content/`:
   - `cp -r docs/ palimpsest/content/docs/`
   - `cp -r templates/ palimpsest/content/templates/`
   - `cp -r agents/ palimpsest/content/agents/`
   - `cp -r playbooks/ palimpsest/content/playbooks/`
   - `cp -r showcase/ palimpsest/content/showcase/`
   - `cp -r case-study/ palimpsest/content/case-study/`
   - Each copied directory must have an `__init__.py` file (empty, for setuptools package discovery)

6. **Create palimpsest/content/__init__.py** with importlib.resources helpers:
```python
"""Access bundled palimpsest content (docs, templates, agents, playbooks, showcase, case-study)."""

from importlib.resources import files
from pathlib import Path

CONTENT_CATEGORIES = [
    "docs",
    "templates",
    "agents",
    "playbooks",
    "showcase",
    "case-study",
]

def get_content_path(category: str, filename: str) -> Path:
    """Get path to a bundled content file.

    Args:
        category: One of docs, templates, agents, playbooks, showcase, case-study.
        filename: File name within the category directory.

    Returns:
        Path to the content file.

    Raises:
        FileNotFoundError: If the category or file does not exist.
    """
    if category not in CONTENT_CATEGORIES:
        raise FileNotFoundError(
            f"Unknown content category '{category}'. "
            f"Available: {', '.join(CONTENT_CATEGORIES)}"
        )
    resource = files("palimpsest.content").joinpath(category, filename)
    path = Path(str(resource))
    if not path.exists():
        raise FileNotFoundError(f"Content file not found: {category}/{filename}")
    return path


def list_content(category: str) -> list[str]:
    """List files in a content category.

    Args:
        category: One of docs, templates, agents, playbooks, showcase, case-study.

    Returns:
        Sorted list of file names in the category.
    """
    if category not in CONTENT_CATEGORIES:
        raise FileNotFoundError(
            f"Unknown content category '{category}'. "
            f"Available: {', '.join(CONTENT_CATEGORIES)}"
        )
    resource_dir = files("palimpsest.content").joinpath(category)
    return sorted(
        item.name
        for item in resource_dir.iterdir()
        if not item.name.startswith("__")
    )
```

7. **Add __init__.py to every content subdirectory** (empty files):
   - `palimpsest/content/docs/__init__.py`
   - `palimpsest/content/templates/__init__.py`
   - `palimpsest/content/agents/__init__.py`
   - `palimpsest/content/playbooks/__init__.py`
   - `palimpsest/content/showcase/__init__.py`
   - `palimpsest/content/case-study/__init__.py`
   - Also: any subdirectories within agents/ (claude-code/, cursor/, cline/, memory-bank/, etc.) need `__init__.py`
  </action>
  <verify>
    <automated>
ls palimpsest/__init__.py palimpsest/cli/__init__.py palimpsest/cli/main.py palimpsest/content/__init__.py && echo "--- structure OK ---" && ls palimpsest/content/docs/01-problem-statement.md palimpsest/content/templates/prd.md palimpsest/content/agents/README.md palimpsest/content/playbooks/README.md palimpsest/content/showcase/quick-wins.md palimpsest/content/case-study/wolt-crm-migration.md && echo "--- content OK ---" && python -c "from importlib.resources import files; d = files('palimpsest.content').joinpath('docs'); print(len([f for f in d.iterdir() if f.name.endswith('.md')])); assert len([f for f in d.iterdir() if f.name.endswith('.md')]) >= 10"
    </automated>
  </verify>
  <acceptance_criteria>
    - palimpsest/__init__.py exists and contains `__version__ = "0.1.0"`
    - palimpsest/cli/main.py exists and contains `def run()` and `app = typer.Typer(`
    - palimpsest/content/__init__.py exists and contains `def get_content_path(` and `def list_content(`
    - palimpsest/content/docs/ contains at least 10 .md files (01-problem-statement.md through 10-privacy-model.md)
    - palimpsest/content/templates/ contains at least 7 .md files (prd.md, brd.md, etc.)
    - palimpsest/content/agents/ contains README.md and subdirectories (claude-code/, cursor/, cline/, memory-bank/)
    - palimpsest/content/playbooks/ contains at least 4 .md files
    - palimpsest/content/showcase/ contains at least 3 .md files
    - palimpsest/content/case-study/ contains wolt-crm-migration.md
    - Every directory under palimpsest/ that contains .py or .md files also has an __init__.py
    - palimpsest.egg-info/ directory does NOT exist (was cleaned)
  </acceptance_criteria>
  <done>Package directory structure exists with all content copied and __init__.py markers in place. importlib.resources can enumerate content files.</done>
</task>

<task type="auto">
  <name>Task 2: Update pyproject.toml with entry points, dependencies, and package-data</name>
  <files>pyproject.toml</files>
  <read_first>
    pyproject.toml
  </read_first>
  <action>
Update pyproject.toml with the following changes (preserve all existing fields not mentioned):

1. **Update [project.scripts]** — replace the single `pac` entry with three entry points:
```toml
[project.scripts]
pal = "palimpsest.cli.main:run"
palimpsest = "palimpsest.cli.main:run"
pac = "palimpsest.cli.main:run"
```

2. **Update [project.dependencies]** — add typer and rich, keep existing deps:
```toml
dependencies = [
    "typer>=0.24.0",
    "rich>=14.0.0",
    "google-api-python-client>=2.100.0",
    "google-auth-httplib2>=0.1.1",
    "google-auth-oauthlib>=1.1.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
```

3. **Update [tool.setuptools.packages.find]** — include both new palimpsest package and existing scripts:
```toml
[tool.setuptools.packages.find]
include = ["palimpsest*", "scripts*"]
```

4. **Add [tool.setuptools.package-data]** — glob patterns for all bundled content:
```toml
[tool.setuptools.package-data]
"palimpsest.content" = [
    "**/*.md",
    "**/*.template",
    "**/*.mdc",
    "**/*.json",
]
```

5. **Update [tool.pytest.ini_options]** — add tests/ to testpaths alongside existing scripts/tests:
```toml
[tool.pytest.ini_options]
testpaths = ["tests", "scripts/tests"]
pythonpath = ["scripts"]
```

6. **Reinstall the package in editable mode:**
```bash
pip install -e ".[dev]"
```
  </action>
  <verify>
    <automated>
pip install -e ".[dev]" 2>&1 | tail -5 && echo "--- install OK ---" && pal --help && echo "--- pal OK ---" && palimpsest --help && echo "--- palimpsest OK ---" && python -m pytest scripts/tests/ -x -q --tb=short 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `pal = "palimpsest.cli.main:run"`
    - pyproject.toml contains `palimpsest = "palimpsest.cli.main:run"`
    - pyproject.toml contains `pac = "palimpsest.cli.main:run"`
    - pyproject.toml contains `"typer>=0.24.0"` in dependencies
    - pyproject.toml contains `"rich>=14.0.0"` in dependencies
    - pyproject.toml contains `include = ["palimpsest*", "scripts*"]`
    - pyproject.toml contains `"palimpsest.content"` in [tool.setuptools.package-data]
    - pyproject.toml contains `testpaths = ["tests", "scripts/tests"]`
    - `pip install -e ".[dev]"` succeeds without errors
    - `pal --help` exits 0 and prints usage text
    - `palimpsest --help` exits 0 and prints usage text
    - `python -m pytest scripts/tests/ -x -q` passes (existing tests not broken)
  </acceptance_criteria>
  <done>Package installs cleanly with all three entry points working. Existing test suite passes. Content is bundled and discoverable via importlib.resources.</done>
</task>

</tasks>

<verification>
1. `pip install -e ".[dev]"` completes without errors
2. `pal --help` prints help text and exits 0
3. `palimpsest --help` prints help text and exits 0
4. `pac --help` prints help text and exits 0 (now points to new CLI)
5. `python -c "from palimpsest.content import list_content; print(list_content('docs'))"` lists 10+ docs
6. `python -c "from palimpsest.content import get_content_path; p = get_content_path('templates', 'prd.md'); print(p)"` returns valid path
7. `python -m pytest scripts/tests/ -x -q` passes (no regression)
</verification>

<success_criteria>
- User can run `pip install -e .` and get working `pal`, `palimpsest`, and `pac` commands
- All methodology content is physically present inside palimpsest/content/ and discoverable at runtime
- Existing scripts/tests/ pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation/01-01-SUMMARY.md`
</output>
